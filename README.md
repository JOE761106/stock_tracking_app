hello this is the first version of the stock tracker it will include:
  -5 stocks
  -api for stocks
  -some photos (2)
  -ui and logic/data in diffrent classes
  -photos will show up based on if the price went up or down so if up stocks.png if down not_stoncks.png
  -stock data is saved to json 
  -if price higher then last price text is green if price went lower text color is red
  -more to come...
  second version will include more things
needed packages:
  requests
  tiknter 
